#算法
####时间复杂度下界:
		对于下标i<j,如果A[i]>a[j],则称(i,j) 是一堆逆序对 
			例如: 3,1,2   3与2,3与1是逆序对,1与2是正序对
			交换2个相邻元素正好消去一个逆序对
		插入排序:T(N,I) = O(N+I)        N为序列个数,I为逆序对
		定理: 任意N个不同元素组成的序列平均具有N(N-1)/4个逆序对
			  任何仅以交换相邻两元素来排序的算法,其平均时间复杂度为Ω(N**2),Ω为下界.
				意味着:要提高效率,必须
				1.每次消去不止1个逆序对!
				2.每次交换相隔较远的2个元素!